# 项目结构

## 模块化组织

```
lanGameSync/
├── main.py                     # 程序入口
├── config.py                   # 配置文件
│
├── managers/                   # 后台服务管理
│   ├── sync_controller.py     # 同步控制器
│   ├── syncthing_manager.py   # Syncthing管理
│   └── easytier_manager.py    # Easytier管理
│
├── ui/                         # UI模块
│   ├── main_window_v2.py      # 主窗口（3400+行，待继续拆分）
│   ├── styles.py              # 样式定义
│   │
│   ├── components/            # UI组件
│   │   ├── message_box.py    # 消息框辅助类 ✅
│   │   └── dialogs/          # 对话框组件
│   │       ├── launcher_selector.py  # 启动器选择 ✅
│   │       ├── peer_manager.py       # 节点管理 🔲
│   │       └── saves_selector.py     # 存档选择 🔲
│   │
│   └── minecraft/             # Minecraft功能
│       ├── launcher_handler.py  # 启动器处理 ✅
│       └── path_resolver.py     # 路径解析 ✅
│
├── utils/                     # 工具模块
│   ├── logger.py              # 日志
│   ├── config_cache.py        # 配置缓存
│   └── process_helper.py      # 进程辅助
│
└── resources/                 # 资源文件
    ├── syncthing.exe
    ├── easytier-core.exe
    └── easytier-cli.exe
```

## 重构进度

### ✅ 已完成

1. **消息框模块** (`ui/components/message_box.py`)
   - 统一管理QMessageBox调用
   - 避免打包后作用域问题
   - 42行，5个静态方法

2. **Minecraft启动器处理** (`ui/minecraft/launcher_handler.py`)
   - 检测启动器类型
   - 查找Minecraft目录
   - 扫描存档
   - 129行，4个静态方法

3. **Minecraft路径解析** (`ui/minecraft/path_resolver.py`)
   - 智能路径映射
   - 支持版本隔离/标准路径
   - 119行，2个静态方法

4. **启动器选择对话框** (`ui/components/dialogs/launcher_selector.py`)
   - 完整的拖放支持
   - 文件浏览选择
   - 194行，独立组件

### 🔲 待拆分

1. **节点管理对话框** (约270行)
   - PeerManagerDialog
   - PeerEditDialog

2. **存档选择对话框** (约300行)
   - SavesSelectorDialog
   - 多选列表
   - 同步配置

3. **主窗口优化** (3400+行 → 目标<2000行)
   - 提取网络管理逻辑
   - 提取游戏管理逻辑
   - 提取UI创建方法

## 重构效果

### 代码质量提升

- ✅ 单一职责原则
- ✅ 低耦合高内聚
- ✅ 易于测试
- ✅ 易于维护

### 可维护性

- 每个模块职责清晰
- 修改影响范围小
- 新功能添加方便

### 性能

- 模块化不影响运行性能
- 打包大小基本不变（~63MB）

## Git提交记录

```
73bf78a feat: 添加启动器选择对话框组件
dcfc524 refactor: 模块化重构 - 添加组件和功能模块
```

## 下一步计划

1. 提取 PeerManagerDialog 到独立文件
2. 提取 SavesSelectorDialog 到独立文件
3. 继续拆分主窗口（目标减少到2000行以下）
4. 添加单元测试
